```mermaid
flowchart
%% the actual pieces
    start((Start))
    clear(Clear the terminal)
    console(Create a new console object - for formating)
    restaurantFilenames(create filename called )
%% connections
```